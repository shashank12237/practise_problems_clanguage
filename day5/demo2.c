#include<stdio.h>
    // #define greater(a,b) (a<b) ? a:b
    // int main()
    // {
    //  int x = 1333, y = 184;
    //  if(greater(x,y))
    //   printf("sun");
    // else
    //  printf("beam");
    int main()
    {
     int x = 10000, y = 1000;
     if(x<y?x:y)
      printf("sun");
    else
     printf("beam");
    return 0;
    }